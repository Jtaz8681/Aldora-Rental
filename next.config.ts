import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Removed custom webpack configuration to avoid potential conflicts.
  // If component tagging is needed, it can be re-added carefully.
};

export default nextConfig;